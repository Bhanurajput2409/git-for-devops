**Launch a instance 
**
.T2-micro for free tier application.

Install Docker in your server

. Sudo apt-get update
. Sudo apt-get docker.io

After Docker installation  and upgradation Docker cmd..

.Docker pull Jenkins/Jenkins:latest

Note- Always use Jenkins/Jenkins not use simple Jenkins

.docker run -d Jenkins/Jenkins:latest

Then your jenkins has been installed in your system through docker.
